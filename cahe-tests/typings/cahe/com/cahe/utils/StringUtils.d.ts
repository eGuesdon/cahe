export declare class StringUtils {
    static prefill(stringChain: string, prefillChar: string, finalLength: number): string;
    static prefillWith(stringChain: string, prefillChain: string): string;
}
